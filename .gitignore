build/
*.o
*.so
*.so.*
*.a
*.pc
*.gir
*.typelib
*.d
src/gowl-version.h
src/protocols/*-protocol.h
*-protocol.h
*-protocol.c
# The above patterns target wayland-scanner OUTPUT (e.g.
# gowl-input-capture-v1-protocol.{c,h}).  The hand-written server-glue
# source files are NOT generated -- keep them tracked.
!src/protocols/gowl-input-capture-protocol.c
!src/protocols/gowl-input-capture-protocol.h
# Standalone tool binaries -- build artifacts produced from tracked source
# by each tool's Makefile (the binary is named after its directory).
/tools/xdg-desktop-portal-gowl/xdg-desktop-portal-gowl
/tools/gowl-mcp/gowl-mcp
.mcp.json
.cache/
compile_commands.json
